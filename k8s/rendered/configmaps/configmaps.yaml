apiVersion: v1
data:
  my.cnf: |
    [mysqld]
    # InnoDB settings
    innodb_buffer_pool_size = 1G
    innodb_log_file_size = 256M
    innodb_flush_method = O_DIRECT

    # Character set and collation
    character-set-server = utf8mb4
    collation-server = utf8mb4_unicode_ci

    # Connection settings
    max_connections = 200

    # Query cache (disabled in MySQL 8.0+, but kept for reference)
    # query_cache_size = 0
    # query_cache_type = 0

    # Logging
    slow_query_log = 1
    slow_query_log_file = /var/log/mysql/slow-query.log
    long_query_time = 2
kind: ConfigMap
metadata:
  annotations:
    helm.sh/chart: k8s-compliment-shop
    managed-by: argocd
  labels:
    app: mysql
    app.kubernetes.io/instance: k8s-compliment-shop
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: k8s-compliment-shop
    app.kubernetes.io/part-of: k8s-compliment-shop
    app.kubernetes.io/version: 1.0.0
    environment: production
    helm.sh/chart: k8s-compliment-shop-1.0.0
    tier: database
  name: mysql-config
---
apiVersion: v1
data:
  bff-service-url: http://bff-service:8082/graphql
  ga-measurement-id: G-BC83RF4L1J
kind: ConfigMap
metadata:
  annotations:
    helm.sh/chart: k8s-compliment-shop
    managed-by: argocd
  labels:
    app: web-service
    app.kubernetes.io/instance: k8s-compliment-shop
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: k8s-compliment-shop
    app.kubernetes.io/part-of: k8s-compliment-shop
    app.kubernetes.io/version: 1.0.0
    environment: production
    helm.sh/chart: k8s-compliment-shop-1.0.0
    tier: frontend
  name: web-service-config
